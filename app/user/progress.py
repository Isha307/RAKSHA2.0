#backend pe firebase se sscores fetch karne wala system

session_id = "or username"

#fetch variables like:
score_in_this_Session = 0 #initialize fresh 
total_score = "fetch_old_one"

if("user enter practice"):
 current_score = "comes from backend ka score.py"
 score_in_this_Session += current_score


total_score += score_in_this_Session